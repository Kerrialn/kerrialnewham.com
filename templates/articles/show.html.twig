{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-12 col-md-6">

                <div class="fs-2">{{ article.title }}</div>
                <div class="text-muted small mb-3">{{ article.publishedAt.diffForHumans }}</div>

                <div class="mb-5">
                    {{ article.content|raw }}
                </div>


            </div>
        </div>
    </div>
{% endblock %}

