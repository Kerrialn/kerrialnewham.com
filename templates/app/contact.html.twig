{% extends 'base.html.twig' %}

    {% block body %}
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-12 col-md-6">

                    <div class="list-group list-group-flush">
                        <div class="list-group-item rounded mb-3 border-0 py-4">
                            <b>Contact</b>
                            <div>
                                Feel free to contact me regarding a project via E-mail or a quick phone call.
                            </div>
                        </div>
                        <div {{ stimulus_controller('clipboard', { successContent: '<div class="bi bi-check-lg text-success fs-4"></div>'|raw }) }}
                                class="list-group-item rounded mb-3 border-0 d-flex justify-content-between align-items-center">
                            <div {{ stimulus_target('clipboard', 'source') }}
                                    data-value="+420608187548">+420 608 187 548
                            </div>
                            <button type="button" {{ stimulus_action('clipboard', 'copy', 'click') }} {{ stimulus_target('clipboard', 'button') }}
                                    class="btn rounded-pill btn-light">
                                <div class="bi bi-copy fs-4"></div>
                            </button>
                        </div>
                        <div {{ stimulus_controller('clipboard', { successContent: '<div class="bi bi-check-lg text-success fs-4"></div>'|raw }) }}
                                class="list-group-item rounded d-flex justify-content-between align-items-center">
                            <div {{ stimulus_target('clipboard', 'source') }}
                                    data-value="kerrialbeckettnewham@gmail.com">kerrialbeckettnewham@gmail.com
                            </div>
                            <button type="button" {{ stimulus_action('clipboard', 'copy', 'click') }} {{ stimulus_target('clipboard', 'button') }}
                                    class="btn rounded-pill btn-light">
                                <div class="bi bi-copy fs-4"></div>
                            </button>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    {% endblock %}

