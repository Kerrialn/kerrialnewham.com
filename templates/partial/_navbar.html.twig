<nav class="navbar navbar-expand-lg mb-3">
    <div class="container">
        <a class="navbar-brand reenie-beanie-regular fs-1" href="{{ path('landing') }}">Kerrial Newham</a>
        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <i class="bi bi-three-dots-vertical fs-3"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') == 'currently' ? 'active fw-bold' }} " aria-current="page" href="{{ path('currently') }}">{{ 'ventures'|trans|title }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') == 'articles' ? 'active fw-bold' }}" aria-current="page" href="{{ path('articles') }}">{{ 'blog'|trans|title }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') == 'about' ? 'active fw-bold' }}" href="{{ path('about') }}">{{ 'about'|trans|title }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') == 'contact' ? 'active fw-bold' }}" href="{{ path('contact') }}">{{ 'contact'|trans|title }}</a>
                </li>
            </ul>
        </div>
    </div>
</nav>